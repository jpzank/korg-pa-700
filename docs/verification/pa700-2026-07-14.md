# PA700 verification — 2026-07-14/15

## Environment

- Input endpoint: `Pa700 KEYBOARD`
- Output endpoint: `Pa700 SOUND`
- Reported firmware: 1.5.0
- Firmware update: not performed

## Identity — Verified

- Sent: `F0 7E 7F 06 01 F7`
- Received: `F0 7E 7F 06 02 42 60 00 5D 00 01 05 00 00 F7`
- Manufacturer: `42`
- Family: `0060`
- Model: `005D`
- Current live retest: passed again on 2026-07-15 after USB hot-plug

During the sandbox follow-up the PA700 temporarily stopped enumerating after a cable disconnect. Reseating the cable in the `USB DEVICE` port restored macOS enumeration and the live identity response. A subsequent input test received three Note On and three Note Off messages for C–E–G on channel 1. The AIArranger sandbox then recognized chord `C`, played its demo style, executed Stop and Panic, closed cleanly, and left no stuck notes by physical confirmation.

## ArrangerLab MIDI Preset — Persisted

- Created in empty user slot `14` as `ArrangerLab`; no factory or existing user preset was overwritten.
- MIDI In: channel 1 Upper 1, channel 2 Upper 2, channel 3 Upper 3, channel 4 Lower, channels 5–15 Off and channel 16 Control.
- MIDI Out: channel 1 Upper 1, channel 2 Upper 2, channel 3 Upper 3, channel 4 Lower, channels 5–15 Off and channel 16 Control.
- MIDI In and MIDI Out filters are all Off, including the previous Sys. Exclusive filters.
- The preset remained selectable after USB reconnection. With it selected, the app passed a fresh identity query, channel-1 Note On/Off exchange and channel-16 SongBook 9000 selection.

## Volume right/layer 1 — Verified

- Outgoing channel-1 bytes: `B0 07 20`, `B0 07 40`, `B0 07 5F`.
- RMS: −52.23, −47.27, −43.38 dBFS; strict increase and 8.85 dB total.
- ArrangerLab MIDI preset and audible change were physically confirmed during the experiment; the persisted slot-14 preset was reselected for the final live retest.

## Expression right/layer 1 — Verified

- Outgoing channel-1 bytes: `B0 0B 20`, `B0 0B 40`, `B0 0B 5F`; CC7 remained fixed at 75% during every stimulus.
- Silence calibration: −63.03 dBFS. Expression RMS: −62.46, −59.90 and −54.72 dBFS; strict increase and 7.75 dB total.
- The app restored CC11 to 127 after every clip. The user physically confirmed that the 25% level was audibly very low.
- Firmware 1.5.0, PA700 identity and the persisted `ArrangerLab` MIDI preset were all reconfirmed by the verifier.
- Canonical package: `Part-Expression-2026-07-16T00-37-03Z.arrlab` in Application Support, including four mono/48 kHz WAVs, raw MIDI, CSV and SMF exports.

## Pan right/layer 1 — Verified

- Outgoing channel-1 bytes: left `B0 0A 00`, center `B0 0A 40`, right `B0 0A 7F`.
- The user physically confirmed the center, left and right stereo positions. The app restored CC10 to center (64) after every clip.
- Silence calibration was −44.78 dBFS. Position RMS was −51.44, −50.16 and −50.02 dBFS; because the evidence source is a mono MacBook microphone and ambient fan noise was significant, these values are retained only as an audit trail and are not treated as directional proof.
- Firmware 1.5.0, PA700 identity and the persisted `ArrangerLab` MIDI preset were reconfirmed by the verifier.
- Canonical package: `Part-Pan-2026-07-16T00-50-15Z.arrlab` in Application Support, including four mono/48 kHz WAVs, raw MIDI, CSV and SMF exports.

## Exact preset — Verified

### Classic Piano

- A1 `Classic Piano`: `CC0=121`, `CC32=4`, `PC=0`, channel 1.
- B `Jimmy Organ`: `CC0=121`, `CC32=13`, `PC=18`, channel 1.
- A2 `Classic Piano`: `CC0=121`, `CC32=4`, `PC=0`, channel 1.
- Averaged spectral distances: A1–A2 0.078398, A1–B 0.386705, A2–B 0.417488.
- Displayed names physically confirmed.
- Canonical package: `Preset-ABA-2026-07-15T17-01-13Z.arrlab` in Application Support.

### Jimmy Organ

- A1 `Jimmy Organ`: `CC0=121`, `CC32=13`, `PC=18`, channel 1.
- B `Classic Piano`: `CC0=121`, `CC32=4`, `PC=0`, channel 1.
- A2 `Jimmy Organ`: `CC0=121`, `CC32=13`, `PC=18`, channel 1.
- Spectral distances: A1–A2 0.155502, A1–B 0.329734, A2–B 0.471779; the repeated Jimmy Organ was the closest pair.
- The PA700 displayed `Jimmy Organ` after the captured panel selection and after the exact bytes were resent independently; the user physically confirmed both returns.
- After rebuilding the app with the promoted profile, the simple **Tocar** page sent Classic Piano and then Jimmy Organ; the PA700 finished on `Jimmy Organ`, physically confirmed by the user.
- The original capture exposed a decoder artifact: status-less bytes inside a PA700 SysEx response were initially interpreted as a later `PC=0`. The extractor now accepts only canonical raw status bytes, is regression-tested, and revalidated the actual ordered `121.13.18 → 121.4.0 → 121.13.18` messages without changing the raw capture.
- Canonical package: `Preset-ABA-Jimmy-Organ-Verified-2026-07-16T03-51-36Z.arrlab` in Application Support. The original Draft package remains preserved.

### Grand Piano

- A1 `Grand Piano`: `CC0=121`, `CC32=3`, `PC=0`, channel 1.
- B `Jimmy Organ`: `CC0=121`, `CC32=13`, `PC=18`, channel 1.
- A2 `Grand Piano`: `CC0=121`, `CC32=3`, `PC=0`, channel 1.
- Spectral distances: A1–A2 0.1674, A1–B 0.2887, A2–B 0.1831; the repeated Grand Piano was the closest pair.
- Displayed names and the exact Upper1 selections were physically confirmed.
- After the profile was promoted and the app rebuilt, the simple **Tocar** page resent `121.3.0`; the PA700 remained on `Grand Piano`, physically confirmed by the user.
- Canonical package: `Preset-ABA-2026-07-16T04-47-08Z.arrlab` in Application Support. The earlier marginal Draft `Preset-ABA-2026-07-16T04-22-09Z.arrlab` remains preserved.

## Arranger Start/Stop — Verified

- Style Play with Clock Source temporarily set to `External USB`; `Clock Send` remained off.
- Captured: one Start `FA`, 2010 Clock `F8`, three Stop `FC`, 32 All Notes Off and 32 All Sound Off events.
- User physically confirmed that the Arranger started and stopped.
- Audio: 41.91-second mono/48 kHz WAV, RMS −34.52 dBFS.
- Clock Source physically restored to `Internal` before promotion.
- Canonical package: `Arranger-Transport-2026-07-15T17-27-58Z.arrlab` in Application Support.

## MIDI Clock — Verified

- The same canonical Arranger transport experiment captured 2010 `F8` pulses at 120 BPM after `FA`, followed by `FC` and Panic cleanup.
- The PA700 physically followed `External USB` clock; the user confirmed Start and Stop, and `Clock Source` was restored to `Internal`.
- The operational `midiClock` transport action exposes `FA`, `FB` and `FC`; the transport scheduler owns continuous `F8` generation at 24 PPQN.

## Keyboard part routing — Physically confirmed

- MIDI preset `14 ArrangerLab`: channel 1 → Upper 1, channel 2 → Upper 2, channel 3 → Upper 3, channel 4 → Lower, channel 16 → Control.
- Direct three-second Note On/Off probes were heard and confirmed for Upper 1 (`90/80`), Upper 2 (`91/81`), Upper 3 (`92/82`) and Lower (`93/83`).
- Upper parts used C5; Lower used C3 with Split enabled. The PA700 remained on firmware 1.5.0.

## SongBook — Verified

- Dedicated internal entry created without overwriting existing entries: `9000 — ArrangerLab Test`.
- Outgoing channel-16 bytes: `BF 63 02`, `BF 62 40`, `BF 06 5A`, `BF 26 00`.
- Style Play and displayed entry physically confirmed.
- User-supplied panel photo shows `SBook: ArrangerLab Te...` after the remote selection.
- Canonical package: `SongBook-9000-2026-07-15T18-06-22Z.arrlab` in Application Support.

## Keyboard Set 1–4 — Verified

- Official PA700 v1.5 mapping: Program Change 64–67 on the configured Control channel 16.
- `CF 41` selected SongBook Keyboard Set 2; the supplied panel photo shows `Kbd2` and the displayed set name `Organ`.
- `CF 43` selected SongBook Keyboard Set 4; the supplied panel photo shows `Kbd4` and the displayed set name `Harmonica`.
- `CF 40` was sent afterward to restore Keyboard Set 1 (`My Setting`).
- Firmware 1.5.0, Style Play and the persisted `ArrangerLab` MIDI Preset remained in use.

## Factory Style catalogue — Verified

- The bundled catalogue contains all 379 factory Styles from the official PA700 v1.5 Musical Resources appendix, pages 942–947.
- Every entry has a unique `CC00.CC32.PC` address and compiles to three scheduled messages on Control channel 16.
- `Bossa Nova` was sent live as `BF 00 00`, `BF 20 06`, `CF 02` (`0.6.2`).
- The user physically confirmed that the Style selection and rhythms work on the PA700. The full officially documented factory Style catalogue is therefore exposed through the operational API.

## USER Style JPD and direct show setup — Verified

- The official PA700 v1.5 Musical Resources table defines User Styles as `CC0=2`, with `CC32=0...11` for User banks 1...12 and `PC=0...47` for their slots.
- `JPD` in `STYLE → USER → JPD` (User bank 11, slot 1) was sent as `BF 00 02`, `BF 20 0A`, `CF 00` (`2.10.0`) on Control channel 16.
- For an unambiguous A–B test, Arranger Lab first selected `Brush Ballad` (`0.1.4`) and then sent `JPD` (`2.10.0`). The user physically confirmed that the PA700 returned to Style `JPD`.
- JPD Keyboard Set 1 is `Piano`; Keyboard Set 2 is `Rhodes`. The verified Control-channel commands are `CF 40` and `CF 41`.
- Master transpose uses Universal Realtime Master Coarse Tuning. Transpose −3 is `F0 7F 7F 04 04 00 3D F7`; the user physically confirmed −3 on the PA700 display.
- Show Mode now sends Style JPD, Keyboard Set and transpose in that order from one click. SongBook remains available as a fallback but is not required for the Showboat Piano Block A.

## Keyboard Set Library catalogue — Verified

- The bundled catalogue contains all 298 factory Keyboard Sets from the official PA700 v1.5 Musical Resources appendix, pages 948–952.
- Every entry has a unique address; examples regression-tested are `Concert Grand = 16.0.0` and `Jimmy Organ = 16.1.0`.
- The driver compiles each selection to `CC0`, `CC32`, then Program Change on Control channel 16.
- `Jimmy Organ` was sent live as `BF 00 10`, `BF 20 01`, `CF 00` (`16.1.0`).
- The user physically confirmed that `Jimmy Organ` appeared on the PA700 display. The full officially documented factory Keyboard Set Library is therefore exposed through the operational API.

## Expanded Control-channel Arranger Elements — Verified

- Variation 1 is individually Verified: `CF 53` selected Variation 1 and the user physically confirmed the panel indication.
- Variation 2 is individually Verified: after reconnecting USB and rechecking identity, `CF 54` selected Variation 2 and the user physically confirmed its panel indicator.
- Variation 3 is individually Verified: in the same stable session, `CF 55` selected Variation 3 and the user physically confirmed its panel indicator.
- Variation 4 is individually Verified: in the same stable session, `CF 56` selected Variation 4 and the user physically confirmed its panel indicator.
- Intro 3 / Count In is individually Verified: with the arranger physically confirmed stopped, `CF 52` lit the Intro 3 / Count In indicator; the following `CF 67` produced one audible count-in measure before Variation 1. The user physically confirmed both selection and playback.
- Intro 2 is individually Verified: `CF 51` lit Intro 2; the following verified Start command played the introduction before entering the Variation.
- Intro 1 is individually Verified after a controlled retest: Stop + Panic, `CF 50`, then verified Start produced an audible introduction before the Variation. The earlier inconclusive attempts remain preserved in the profile evidence.
- Fill 1–4 are individually Verified: `CF 57`–`CF 5A` each produced an audible Fill during internal-clock playback. Fill 1 was also observed armed while stopped.
- Break is individually Verified: `CF 5B` produced an audible pause/transition and the rhythm resumed.
- Ending 1–3 are individually Verified: `CF 5C`–`CF 5E` each played a distinct ending and stopped the arranger automatically.
- Arranger Start/Stop through the internal clock is individually Verified: the first `CF 67` started Brush Ballad at 73 BPM, the second `CF 67` stopped it, and Panic followed. The user physically confirmed both transitions.
- All 15 Arranger Elements on Control channel 16 are now individually Verified.

## Contextual Arranger controls — 9/9 Verified

- Fade In/Out `CF 5F`: while stopped, started Brush Ballad with a gradual fade-in; during playback, a second send produced a gradual fade-out and automatic stop.
- Style to Kbd Set `CF 60`: three-state cycle physically confirmed as off → steady on → blinking → off.
- Auto Fill `CF 61`: physical indicator toggled off and back on.
- Memory `CF 62`: physical indicator established on, then toggled off.
- Bass Inversion `CF 63`: in the Style Play **Split** pane, the checkbox toggled on and off.
- Manual Bass `CF 64`: did not activate without an active keyboard split; with the physical **SPLIT** indicator on, Manual Bass toggled on and off.
- Tempo Lock `CF 65`: physical lock indicator toggled on and off.
- Arranger Start/Stop `CF 67`: started and stopped Brush Ballad under the internal clock, as recorded above.
- Player Play/Stop `CF 68`: with Song Play and the internal DISK song **Late 1** selected, the first send started Player playback and the second send stopped it; the user physically confirmed both transitions.
- All checks used firmware 1.5.0, Style Play, Control channel 16 and the persisted `ArrangerLab` MIDI Preset. The user physically confirmed every promoted transition.

The original five PA700 vertical mappings, per-part Expression, Pan and Damper, MIDI Clock, Keyboard Set selection, all Arranger Elements, all nine documented contextual Arranger/Player controls, Classic Piano and Jimmy Organ have the required physical evidence and are exposed through the operational driver. No promoted PA700 mapping remains Draft. The slot-14 `ArrangerLab` MIDI Preset is persisted.

## Version 1.0.0 release smoke test — Passed

- Date: 2026-07-17.
- The packaged Release build interpreted `Jimmy Organ com Brush Ballad na variação 3`, displayed the expected Verified before/after preview and applied it only after explicit confirmation.
- The user physically confirmed Jimmy Organ, Brush Ballad and Variation 3 on the PA700 with MIDI Preset `14 ArrangerLab` active.
- Panic completed and the user confirmed that no note remained stuck.
